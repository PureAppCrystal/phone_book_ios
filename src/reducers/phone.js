import * as types from 'actions/ActionTypes';

const initialState = {
    id: -1,
    name: '',
    number: '',
    phones: [
    ]
}

function phone(state = initialState, actions) {
    const{ phones } = state;

    switch(actions.type){
        
    }
}